define([
		'underscore',
		'backbone'
	],
	function(_, Backbone) {
		// "use strict";

		
		return Backbone.Model.extend({
			idAttribute: 'modelId',
			initialize : function() {
				
			}
		});
	}
);
